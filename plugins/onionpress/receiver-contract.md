# Moved

The wire contract lives in the OnionPress fork now, as
`docs/static-publish-protocol.md` — rewritten for receiver v2.0 (multipart-only
upload, allowlist trust; the old raw-body carrier this copy described is gone).
This stub stays because other moss docs still link to this path.
