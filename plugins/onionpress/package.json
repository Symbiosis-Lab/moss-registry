{
  "name": "@symbiosis-lab/moss-plugin-onionpress",
  "version": "0.2.0",
  "description": "Publish moss sites to a self-hosted OnionPress (.onion) receiver",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm run clean && npm run bundle && npm run copy-assets",
    "dev": "npm run clean && npm run copy-assets && concurrently \"npm run bundle -- --watch\" \"npm run watch-assets\"",
    "bundle": "esbuild src/main.ts --bundle --format=iife --global-name=OnionpressPlugin --outfile=dist/main.bundle.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "copy-assets": "cp assets/* dist/",
    "watch-assets": "chokidar 'assets/**/*' -c 'npm run copy-assets'",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run --project unit"
  },
  "keywords": [
    "moss",
    "plugin",
    "onionpress",
    "tor",
    "onion",
    "deployment",
    "deployer"
  ],
  "author": "Symbiosis Lab",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/",
    "provenance": false
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Symbiosis-Lab/moss-plugins.git",
    "directory": "onionpress"
  },
  "devDependencies": {
    "@symbiosis-lab/moss-api": "^0.10.0",
    "@types/node": "^20.19.43",
    "@vitest/coverage-v8": "^3.2.7",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.28.1",
    "happy-dom": "20.8.9",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
